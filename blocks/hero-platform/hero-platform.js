/**
 * Hero Platform variant.
 * The hero block is CSS-only in the boilerplate; decoration is handled by
 * default section/block processing. This no-op keeps the block module present
 * and available for future enhancement.
 * @param {Element} block The block element
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {}
